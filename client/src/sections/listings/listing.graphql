query Listings {
    listings {
        id
        title
        image
        address
        price
        numOfGuests
        numOfBeds
        numOfBaths
        rating
    }
}

mutation DeleteListing($id: ID!) {
    deleteListing(id: $id) {
        id
    }
}
